# AGENTS.md

このリポジトリで作業するときの方針。

## 言語

- **日本語で書く。** 応答、PR のタイトルと本文、Issue、ドキュメント、コミットメッセージは原則すべて日本語にする。
- 識別子・コマンド・設定キー・ファイルパス・固有名詞はそのまま英語で書く。無理に訳さない。
- Conventional Commits の prefix（`feat:` `fix:` など）は英語のまま使い、本文を日本語にする。

## skill を書くときの規範

- skill のソースは `skills/<name>/SKILL.md` が単一の真実。配布先の生成物を編集しない。
- skill を作る・直すときは `myskill` に従う。日本語の文章を書くときは `mywriting` に従う。
- 環境固有の記述を書かない。個人のマシンのパス、会社名、認証情報、参照元リポジトリの内部構造は含めない。このリポジトリは public であり、参照元を知らない単位として閉じている。

## 変更の反映

このリポジトリへのマージだけでは、どの環境にも反映されない。
参照側のマニフェストが固定している SHA を書き換えて初めて配布される。
skill を変えたら、参照側の更新まで面倒を見る。
